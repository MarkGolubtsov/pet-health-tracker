#!/usr/bin/env sh
set -eu

cd "$(dirname "$0")/.."

if [ ! -f .env ]; then
  echo "Missing .env. Create it from .env.example first." >&2
  exit 1
fi

. ./version.txt
export PET_HEALTH_TRACKER_VERSION="$version"

exec docker compose "$@"
